<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<title>{{title}}</title>
	<style>
		body {
			margin: 30px;
		}
	</style>
</head>
<body>
	{{{body}}}
	<div style="margin-top: 200px; border-top:1px solid black;">
		<h2>デバッグ用</h2>
		<h3>全ページ</h3>
		{{#each pages}}
			<li><a href="{{link}}">{{text}}ページ</a></li>
		{{/each}}
		<h3 style="margin-top: 2em;">セッション状態</h3>
		<p>
			{{#if session.user}}
				ログイン中 id: {{session.user.userId}}
			{{else}}
				ログインしていない
			{{/if}}
		</p>
	</div>
</body>
</html>